spring:
  datasource:
    url: jdbc:postgresql://localhost:1234/jewellery
    username: test
    password: test
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update  # Use 'update', 'create', 'validate', or 'none' based on your needs
    show-sql: true
    properties:
      hibernate:
        format_sql: true
